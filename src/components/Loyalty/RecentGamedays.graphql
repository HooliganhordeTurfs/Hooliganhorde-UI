query Seasons {
  seasons(first: 24, orderBy: season, orderDirection: desc) {
    id
    season
    price
    deltaHooligans
  }
  fields(first: 24, orderBy: season, orderDirection: desc) {
    id
    season
    rage
    intensity
  }
}